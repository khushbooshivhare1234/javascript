/*var defaultValue = "John Doe"; // Default value for the input field

// Display a prompt dialog box with a message and a default value
var name = prompt("Please enter your name:", defaultValue);

// Check if the user entered a name
if (name !== null) {
    // User entered a name
    alert("Hello, " + name + "!");
} else {
    // User canceled the prompt
    alert("You canceled the prompt.");
}
var gamenum=25;
var unum=prompt("guess number");
while(gamenum!=unum){
    unum=prompt("wrong num guess again");

}
alert("congu");*/
let k =prompt("enter  fullname");
let s="@"+ k + k.length;
console.log(s)
document.write(s);